import React from 'react';

const x = 3;

// eslint-disable-next-line no-console
let hello = a => console.log(`hello, ${a}`);

hello(x);

let jsx = <p className="st" style="color: green;">Hello, JSX!</p>;

export default jsx;
